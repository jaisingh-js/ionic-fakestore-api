import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-fakestore-api',
  webDir: 'docs',
  bundledWebRuntime: false
};

export default config;
